# 雲原生全端專案實作計劃書（前後端分離）

> 目標：完成一個涵蓋「前後端撰寫與 API 串接、資料庫、testing、CI/CD、K8S 部署、串接網域」的小型雲原生專案，作為網頁架構工程師的練習作品。
> 核心原則：**本地能做的別上雲，一次只加一層，每層跑得起來再往下走，雲端資源用完就砍。**

---

## 一、專案概述

### 學習目標對照

| 你的目標 | 對應階段 |
|---|---|
| 前後端撰寫與 API 串接 | Phase 1–2 |
| 資料庫串接 | Phase 1、5、7 |
| Testing | Phase 3 |
| CI/CD | Phase 6–7 |
| 部署在 K8S | Phase 5、7 |
| 串接網域 | Phase 8 |

### 專案範圍建議

把 App 本身做到「無聊」。選一個最單純的 CRUD 應用（**待辦清單 / 書籤管理 / 短網址**任選），schema 越簡單越好（一到兩張表）。你的學習重點是整條 infra pipeline，不是功能，所以功能越簡單，測試與 API 串接越好寫。

> 建議 schema（以待辦清單為例）：`Todo(id, title, done, createdAt)`。一張表就夠了。

---

## 二、技術選型總表

| 環節 | 工具 | 備註 |
|---|---|---|
| 版控 | GitHub | monorepo，前後端分資料夾 |
| 前端 | Vite + React | 獨立 SPA，用 fetch/axios 打後端 API |
| 後端 | Node.js + Express | 最好上手、文件最多 |
| ORM | Prisma | type-safe、migration 好用 |
| 資料庫 | PostgreSQL | 本地用 Docker container，K8S 用 StatefulSet |
| 單元/整合測試 | Vitest | 後端邏輯 + 前端 component |
| E2E 測試 | Playwright | 完整使用者流程 |
| 容器化 | Docker + Docker Compose | 進 K8S 前的必要前置 |
| 本地 K8S | kind 或 k3d | 省錢學 K8S 的關鍵 |
| CI/CD | GitHub Actions | lint → test → build → deploy |
| 雲端部署 | GKE Autopilot | 不用管 node；用完就砍 |
| Image 倉庫 | Artifact Registry (GCP) | CI 推 image 到這 |
| 網域 | Cloudflare Registrar / Namecheap | 買 `.dev` 或 `.app`（強制 HTTPS） |
| Ingress + TLS | GKE Ingress + Google-managed 憑證 | cert-manager 當進階 |
| 監控 | kube-prometheus-stack (Helm) | 一次裝好 Prometheus + Grafana，可選 |

---

## 三、系統架構

請求流向（前後端分離）：

```
使用者瀏覽器
   │
   ▼
Ingress（GKE Ingress / 本地 ingress-nginx）
   ├── "/"      → Frontend Service → Frontend Pod（Nginx 靜態伺服 React build）
   └── "/api/*" → Backend Service  → Backend Pod（Express + Prisma）
                                          │
                                          ▼
                                  Postgres（StatefulSet + PVC）
```

重點：前端、後端是**兩個獨立的工作負載**，各自打包成 image、各自一個 Deployment。前端透過 Ingress 的 `/api` 路徑打後端，藉此避免跨網域 CORS 問題（本地開發階段才需要手動處理 CORS）。

---

## 四、專案結構

```
project-root/
├── frontend/          # Vite + React
│   ├── src/
│   ├── Dockerfile
│   └── package.json
├── backend/           # Node.js + Express + Prisma
│   ├── src/
│   ├── prisma/        # schema.prisma、migrations
│   ├── Dockerfile
│   └── package.json
├── k8s/               # 所有 K8S manifests
│   ├── namespace.yaml
│   ├── secret.yaml
│   ├── configmap.yaml
│   ├── postgres.yaml
│   ├── backend.yaml
│   ├── frontend.yaml
│   └── ingress.yaml
├── .github/
│   └── workflows/     # GitHub Actions
├── docker-compose.yml
└── README.md
```

---

## 五、分階段執行計劃

### Phase 0 — 環境與版控準備

**目標**：把工具鏈與 repo 準備好。

- [ ] 本地安裝：Docker Desktop、Node.js（LTS）、`kubectl`、`kind`（或 `k3d`）、`helm`
- [ ] 建立 GitHub repo，設定好 `.gitignore`（排除 `node_modules`、`.env`、`dist`）
- [ ] 依「專案結構」建好資料夾骨架
- [ ] 寫一份 README，先把本計劃的階段列進去當進度追蹤

**驗收標準**：`docker --version`、`kubectl version --client`、`kind --version`、`node --version` 都能正常回應。

> 注意：`.env` 絕對不要 commit。第一天就把它加進 `.gitignore`。

---

### Phase 1 — 後端 API + 資料庫（本地）

**目標**：做出能對 PostgreSQL 進行 CRUD 的 REST API。

- [ ] 在 `backend/` 執行 `npm init`，安裝 Express、Prisma（dev）、@prisma/client、cors
- [ ] 執行 `npx prisma init`，在 `schema.prisma` 設定 datasource 為 postgresql，定義 Todo model
- [ ] 本地用 Docker 起一個 PostgreSQL container（指定 port、密碼、掛 volume）
- [ ] 在 `.env` 設定 `DATABASE_URL` 指向本地 Postgres
- [ ] 執行 `npx prisma migrate dev --name init` 建表並產生 Prisma Client
- [ ] 撰寫 Express 應用：一支健康檢查路由 `/health`（回 200）+ CRUD 路由 `/api/todos`（GET/POST/PUT/DELETE），內部呼叫 Prisma Client
- [ ] 掛上 CORS middleware，允許前端 origin（開發期）
- [ ] 本地啟動後端，用 `curl` 或 Postman 測試每一支 API

**驗收標準**：能透過 API 新增/查詢/更新/刪除待辦，資料確實寫入 DB（可用 `npx prisma studio` 檢視）。

> 常見陷阱：忘了 `prisma generate`（migrate 會順帶做）；`DATABASE_URL` 寫錯 port 或密碼；CORS 沒開導致前端打不到。

---

### Phase 2 — 前端 + API 串接（本地）

**目標**：React 前端透過 HTTP 打後端 API，完成 CRUD 畫面。

- [ ] 執行 `npm create vite@latest frontend`（選 react 或 react-ts 樣板）
- [ ] （可選）安裝 axios
- [ ] 把 API base URL 放進 Vite 環境變數（`VITE_` 前綴，透過 `import.meta.env.VITE_API_URL` 讀取），**不要 hardcode**
- [ ] 撰寫元件：載入時 `fetch` GET 清單、表單送出時 POST、勾選/刪除時 PUT/DELETE
- [ ] 本地同時啟動前端（5173）與後端（3000），驗證串接

**驗收標準**：畫面能完整 CRUD，瀏覽器 Network 分頁看得到打向後端的請求與正確回應。

> 為什麼堅持用環境變數存 API URL：之後容器化、上 K8S、串網域時，這個 URL 會一直換。寫死你會改到崩潰。

---

### Phase 3 — 測試（Vitest + Playwright）

**目標**：建立單元測試與端對端測試。

- [ ] 後端：安裝 Vitest（可搭配 supertest 打 Express app），為路由/邏輯寫單元或整合測試
- [ ] 前端：安裝 Vitest + Testing Library + jsdom，測元件渲染與互動
- [ ] 安裝 Playwright（`npm init playwright`），撰寫 E2E：開頁 → 新增一筆 → 驗證出現 → 刪除
- [ ] 在各自 `package.json` 設定 `test` scripts
- [ ] 設定 Playwright 的 webServer，讓它跑測試前自動啟動前後端

**驗收標準**：`npm test` 全綠；Playwright 完整流程通過。

> 陷阱：E2E 若沒設 webServer，就得手動先把前後端跑起來才能測；測試用 DB 建議與開發 DB 分開，避免污染資料。

---

### Phase 4 — 容器化（Docker）

**目標**：把前後端各自打包成 image，並用 Compose 一次啟動整包。

- [ ] 撰寫 **backend Dockerfile**：用 Node base image、multi-stage build（build 階段安裝依賴並 `prisma generate`，runtime 階段只保留必要檔案）、宣告連接埠、設定啟動指令
- [ ] 撰寫 **frontend Dockerfile**：build 階段 `npm run build` 產生靜態檔，runtime 用 Nginx 提供靜態服務
- [ ] 撰寫 `docker-compose.yml`：定義 frontend、backend、postgres 三個服務，設定 network、volume、`depends_on`
- [ ] 執行 `docker compose up`，於瀏覽器操作驗證

**驗收標準**：`docker compose up` 一次把整包拉起，功能與本地開發時一致。

> 關鍵陷阱：**Vite 環境變數是 build-time 注入的，不是 runtime**。前端的 `VITE_API_URL` 要在 build image 時就決定，這是最多人卡住的地方。此外 Postgres 一定要掛 volume，否則容器重啟資料就沒了。

---

### Phase 5 — 本地 K8S（kind / k3d）

**目標**：在本地 K8S 叢集把整個系統跑起來（免費、可反覆重來）。

- [ ] 用 `kind create cluster` 建立本地叢集
- [ ] 在 kind 安裝 ingress controller（ingress-nginx）
- [ ] 用 `kind load docker-image` 把本地 build 的 image 載入叢集（本地不需 registry）
- [ ] 撰寫並套用以下 manifests（描述其結構）：
  - **Secret**：DB 帳密、`DATABASE_URL`
  - **ConfigMap**：非機密設定
  - **Postgres**：StatefulSet + PVC（持久化）+ headless Service
  - **Backend**：Deployment（環境變數從 Secret/ConfigMap 注入、宣告 containerPort、設定 readiness/liveness probe 指向 `/health`、設定 resources requests/limits）+ ClusterIP Service
  - **Frontend**：Deployment + ClusterIP Service
  - **Ingress**：`/` 導向 frontend、`/api` 導向 backend
- [ ] 用 `kubectl get pods`、`kubectl logs`、`kubectl describe` 逐一 debug 到全部 Running

**驗收標準**：透過 Ingress 的本地 host 能操作整個應用。

> 陷阱：本地載入的 image 要把 `imagePullPolicy` 設為 `IfNotPresent`，否則 K8S 會去遠端拉取而失敗；前端建議打相對路徑 `/api`（走同一個 Ingress），可完全避開 CORS。

---

### Phase 6 — CI（GitHub Actions）

**目標**：push 後自動測試與建置 image。

- [ ] 在 `.github/workflows/` 建立 workflow，觸發條件為 push / pull request
- [ ] 定義工作流程：安裝依賴 → lint → 跑 Vitest → 建置前後端 Docker image
- [ ] 用 cache 加速 `node_modules` 安裝
- [ ] 此階段先只做「測試 + build」，尚未推送或部署

**驗收標準**：push 後 GitHub Actions 顯示綠燈，測試失敗會擋下。

> 提醒：任何密鑰放 GitHub Secrets，不要寫進 workflow 檔。

---

### Phase 7 — 部署上 GKE

**目標**：把本地驗證過的 manifests 搬上雲端。

- [ ] 建立 GCP 專案並啟用計費（使用 $300 免費試用額度）
- [ ] 啟用 GKE 與 Artifact Registry API，建立一個 Artifact Registry repo
- [ ] 在 CI 加一段：build → tag → push image 至 Artifact Registry
- [ ] GCP 認證建議用 **Workload Identity Federation**（避免長期金鑰，較安全）
- [ ] 建立 **GKE Autopilot** 叢集
- [ ] 將本地 manifests 的 image 路徑改為 Artifact Registry，套用到 GKE
- [ ] CD：Actions 認證 GCP 後執行 `kubectl apply` 或更新 image

**驗收標準**：透過雲端對外 IP / Ingress 能連到應用。

> 成本警示：Ingress 會建立一個 LoadBalancer（要錢）；Autopilot 依 pod 的 resource request 計費；**練完務必刪叢集**。詳見「成本控制檢查表」。

---

### Phase 8 — 串接網域 + HTTPS

**目標**：用自己的網域搭配自動 HTTPS 對外提供服務。

- [ ] 於 Cloudflare Registrar 或 Namecheap 購買網域（建議 `.dev` / `.app`，強制 HTTPS）
- [ ] 在 GCP 保留一個靜態 IP
- [ ] 設定 GKE Ingress（GCE ingress class）搭配 ManagedCertificate 資源（Google-managed 憑證）
- [ ] 在網域 DNS 設定 A record 指向該靜態 IP
- [ ] 等待憑證 provisioning 完成（可能數十分鐘）
- [ ] 把前端的 API URL 改為正式網域

**驗收標準**：`https://你的網域` 能開啟，瀏覽器顯示有效憑證（鎖頭）。

> 陷阱：Google-managed 憑證要等 DNS 指對後才會核發，且 provision 需要時間，看到憑證狀態卡在 provisioning 是正常的，耐心等。想學跨平台通用做法可改用 cert-manager + Let's Encrypt（設定較多，列為進階）。

---

### Phase 9 — 監控（可選，最後做）

**目標**：用 Prometheus + Grafana 觀測叢集。

- [ ] 安裝 Helm
- [ ] 用 Helm 安裝 `kube-prometheus-stack`（一次裝好 Prometheus + Grafana + Alertmanager + exporters）
- [ ] 透過 port-forward 或 Ingress 開啟 Grafana，查看內建的 K8S 儀表板
- [ ] （進階）在後端加一支 `/metrics` 端點供 Prometheus 抓取自訂指標

**驗收標準**：Grafana 能看到叢集與節點的即時指標。

> 提醒：這套很吃資源，會增加 Autopilot 成本，玩完用 `helm uninstall` 移除。

---

## 六、成本控制檢查表

- [ ] 確認使用 **$300 / 90 天**免費試用額度
- [ ] 了解 GKE 每月 **$74.40** 額度只 cover 一個叢集的「管理費」，**pods、LoadBalancer、egress、儲存另計**
- [ ] 練習時採「短時間、密集實驗」，不要讓叢集整天開著
- [ ] 每次收工前確認：不需要的資源已 `kubectl delete` 或整個叢集刪除
- [ ] 在 GCP 設定預算警示（Budget Alert），超過門檻寄信通知

---

## 七、進階延伸題（做完主線再挑戰）

- **Cloud SQL 取代 in-cluster Postgres**：更貼近業界實務，但需設定 Cloud SQL Auth Proxy 與 service account（有成本）
- **cert-manager + Let's Encrypt**：跨平台通用的 TLS 方案，取代 Google-managed 憑證
- **Argo CD / Flux（GitOps）**：把 push-based 部署升級為宣告式 GitOps
- **HPA 自動擴縮**：依 CPU/記憶體或自訂指標自動增減 pod
- **前端走 CDN**：靜態資源改用 Cloud CDN 或物件儲存 + CDN 提供

---

## 八、總體學習檢核清單

- [ ] 我能說明前後端分離架構下，一個請求如何從瀏覽器經 Ingress 到達後端與資料庫
- [ ] 我能獨立寫出一支串接資料庫的 REST API
- [ ] 我能設定並跑通單元測試與 E2E 測試
- [ ] 我能把應用容器化並用 Compose 在本地整包啟動
- [ ] 我能撰寫 Deployment / Service / Ingress / StatefulSet 等 K8S 資源
- [ ] 我能設定 GitHub Actions 自動測試、建置與部署
- [ ] 我能把應用部署到 GKE 並用自己的網域搭配 HTTPS 對外服務
- [ ] 我了解每項雲端資源的成本來源，並知道如何控制帳單
